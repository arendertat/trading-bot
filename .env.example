# Binance API Credentials
BINANCE_API_KEY=your_api_key_here
BINANCE_API_SECRET=your_api_secret_here

# Telegram Notifications (optional)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here

# Config Path (optional, defaults to config/config.json)
CONFIG_PATH=config/config.json

# Live Trading Safety Lock (Bulgu 9.2)
# DANGER: Uncomment and set to "true" ONLY when you intend to trade with real money.
# When config.json has "testnet": false, this env var must be set to "true" or the bot will refuse to start.
# LIVE_TRADING_CONFIRMED=true
